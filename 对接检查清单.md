# 前后端对接检查清单

## ✅ 已完成

- [x] 创建 API 服务层结构
- [x] 封装 HTTP 请求工具（axios）
- [x] 创建三个页面的 API 接口文件
- [x] 添加请求/响应拦截器
- [x] 添加错误处理
- [x] 添加 Token 认证支持
- [x] 创建环境变量配置文件
- [x] 创建使用示例文档

## 📋 待完成（需要你的配合）

### 1. 安装依赖
```bash
npm install axios
```

### 2. 配置后端接口地址
- [ ] 修改 `.env.development` 中的后端地址
- [ ] 修改 `.env.production` 中的后端地址
- [ ] 或者直接修改 `src/utils/request.js` 中的 `baseURL`

### 3. 确认后端接口规范
- [ ] 接口路径是否匹配（如：`/api/report/templates`）
- [ ] 请求方法是否正确（GET/POST/PUT/DELETE）
- [ ] 响应数据格式（是否需要调整 `request.js` 中的响应拦截器）
- [ ] 是否需要 Token 认证
- [ ] 文件上传/下载的接口格式

### 4. 在页面中替换模拟数据
- [ ] ReportManagement.vue - 替换模板列表、创建、更新、删除接口
- [ ] FieldConfig.vue - 替换字段配置列表、保存接口
- [ ] ReportBrowse.vue - 替换表头、数据查询接口

### 5. 测试对接
- [ ] 测试列表查询
- [ ] 测试创建/更新/删除
- [ ] 测试文件上传/下载
- [ ] 测试搜索和分页
- [ ] 测试错误处理

## 🔧 我可以帮你做的

告诉我以下信息，我可以帮你完成对接：

1. **后端接口地址**：你的后端 API 基础地址
2. **接口路径**：如果路径不同，告诉我正确的路径
3. **响应格式**：后端返回的数据格式（code/message/data 结构）
4. **特殊需求**：如特殊的请求头、认证方式等

然后我可以：
- 调整 API 接口路径
- 修改响应拦截器以适配你的数据格式
- 在页面中替换所有模拟数据为真实接口调用
- 添加必要的错误处理和加载状态

## 📝 示例：告诉我你的接口信息

```
后端地址：http://localhost:8080
接口路径：/api/v1/report/templates
响应格式：
{
  "success": true,
  "data": {...},
  "message": "操作成功"
}
```

我会根据你提供的信息调整代码！



